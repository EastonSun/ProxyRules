# ============================================================
# ProxyRules — 手动删除直连域名
# ============================================================
# 一行一个域名，将在 direct_domain.txt 输出中被强制删除
# 即使上游将其归类为直连，也会被移除
# 支持以下格式:
#   example.com              — 完整域名
#   *.example.com            — 通配符
#
# 适用场景:
#   - 某个域名被上游误标为直连，但实际需要代理
#   - 某个 CDN 域名在国内访问不稳定
# ============================================================

googleapis.cn
services.googleapis.cn
xn--ngstr-lra8j.com
