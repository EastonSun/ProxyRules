# ============================================================
# ProxyRules — 手动删除拦截 IP-CIDR
# ============================================================
# 一行一个 CIDR (如 1.0.1.0/24)，将在 reject_ip.txt 输出中被强制删除
# 即使上游将其归类为广告/追踪，也会被排除
#
# 适用场景:
#   - 某个正常 IP 被广告规则误杀（误拦截）
#   - 某个 IP 虽然被标记为广告，但实际上影响到了正常功能
# ============================================================
