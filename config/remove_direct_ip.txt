# ============================================================
# ProxyRules — 手动删除直连 IP-CIDR
# ============================================================
# 一行一个 CIDR (如 1.0.1.0/24)，将在 direct_ip.txt 输出中被强制删除
# 即使上游将其归类为直连，也会被移除
#
# 适用场景:
#   - 某个 IP 段被上游误标为直连，但实际需要代理
# ============================================================
