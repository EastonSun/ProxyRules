# ============================================================
# ProxyRules — 手动删除拦截域名
# ============================================================
# 一行一个域名，将在 reject_domain.txt 输出中被强制删除
# 即使上游将其归类为广告/追踪，也会被排除
#
# 适用场景:
#   - 某个正常域名被广告规则误杀（误拦截）
#   - 某个域名虽然被标记为广告，但实际上影响到了正常功能
# ============================================================
126.net
t.co
